# Extractable components

No shared shell component should be extracted for this target. The existing `ConsultantWidget` is a feature-specific stateful panel and must not appear in CRM. Basic controls are intentionally kept inline.
